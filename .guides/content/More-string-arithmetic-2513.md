On the left is another way of dealing with a message. Notice how the `' '` inserts a space. This is so we get `Michael Smith` and not `MichaelSmith`

Make sure you understand it before we do our next challenge.
